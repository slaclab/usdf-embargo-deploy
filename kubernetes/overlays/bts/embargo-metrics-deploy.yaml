---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bts-embargo-metrics
  labels:
    app: metrics
    site: bts
spec:
  replicas: 1
  selector:
    matchLabels:
      app: metrics
      site: bts
  template:
    metadata:
      labels:
        app: metrics
        site: bts
    spec:
      containers:
      - name: metrics
        image: "ghcr.io/lsst-dm/embargo-butler-ingest:prod"
        env:
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis
              key: redis-password
        resources:
          limits:
            cpu: "1"
            memory: "2Gi"
